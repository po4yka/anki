Run `cargo test --workspace` and report a results summary.

Include:
- Total tests run, passed, failed, ignored
- Any failing test names with their error output
- If all tests pass, confirm with a brief summary
