Run the following build commands and report any errors:

1. `cargo check --workspace` -- type-check all crates
2. `cargo build -p anki_bridge` -- build the Swift FFI bridge

If either command fails, show the full error output and suggest fixes.
If both succeed, confirm with a summary of crates checked.
